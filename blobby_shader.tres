[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform vec4 color; // imported parameter

void fragment() {
	COLOR = texture(TEXTURE, UV); // load the original sprite as is
	if(COLOR.a > 0f) { // for all non transparent pixels
		COLOR = color;
	}
}"
custom_defines = ""

[resource]
shader = SubResource( 1 )
shader_param/color = Plane( 0.1, 0.5, 1, 1 )
